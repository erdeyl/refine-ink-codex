# Legacy Assets

This folder preserves the upstream Claude-specific orchestration files from the original `refine-ink` project.

These files are kept for migration traceability only and are not required for Codex operation.

Use the Codex-native entry point instead:

```bash
python scripts/codex_prepare_review.py path/to/paper.pdf
```
